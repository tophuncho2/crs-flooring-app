# Templates — Data Layer (non-schema)

Brief stub. List row aggregates: `itemCount`, `serviceItemCount`, `salesRepCount`, `workOrderCount`. Rollup totals (material / service / grand) computed from child rows.
