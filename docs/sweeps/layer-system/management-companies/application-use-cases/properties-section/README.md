# Management Companies — Properties Section

Openable drill-down only. Lists child properties and lets the user jump into a property record. No editable section, no use cases — reads flow through the properties module's own readers.
