# Properties — Templates Section

Openable drill-down only. Lists child templates and lets the user jump into a template record. No editable section, no use cases — reads flow through the templates module's own readers.
