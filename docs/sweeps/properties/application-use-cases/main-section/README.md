# Properties — Application Use Cases

Record-level only. No editable child sections — child relations (`workOrders`, `templates`) are read-only openable drill-downs, not section saves.

- `createPropertyUseCase`
- `updatePropertyUseCase`
- `deletePropertyUseCase`
