# Dashboard Loaders — Alterations

Loaders and dropdowns for the dashboard / record views. Grouped by module. Source comments in `../mocks.md`.

## Clients / Properties

- [ ] In the work orders and templates record view, filter the property dropdown to the linked management company when one is selected.

## Main-Hub / Templates

- [ ] Confirm `warehouse` is the dropdown shown in the record view (tied to the `store` column investigation).
- [ ] Use the new `FlooringJobType` as a dropdown in the record view.
