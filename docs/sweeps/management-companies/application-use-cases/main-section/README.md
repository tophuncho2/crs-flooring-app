# Management Companies — Application Use Cases

Record-level only. No editable child sections — the `properties` child relation is a read-only openable drill-down, not a section save.

- `createManagementCompanyUseCase`
- `updateManagementCompanyUseCase`
- `deleteManagementCompanyUseCase`
