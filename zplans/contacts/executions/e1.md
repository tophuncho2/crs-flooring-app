 Migration Complete

  What was done

  Step 1 — Domain Layer (packages/domain/src/flooring/contacts/)
  - Created types.ts (types, validation, form helpers) and index.ts barrel
  - Updated packages/domain/src/index.ts to export via the index barrel

  Step 2 — Data Layer (packages/db/src/flooring/contacts/)
  - Created read-repository.ts with normalizers (normalizeContactRow, normalizeContactDetail), all read queries (listContacts, listSalesRepContactOptions, getContactById, getContactDeleteState), and ContactRecord type
  - Created write-repository.ts with createContactRecord, updateContactRecord, deleteContactRecordById
  - All functions accept optional client parameter matching categories pattern
  - Updated packages/db/src/index.ts

  Step 3 — Application Layer (packages/application/src/flooring/contacts/)
  - Mirrored admin module file-for-file: errors.ts (ContactExecutionError), types.ts, create-contact.ts, update-contact.ts, delete-contact.ts, index.ts
  - deleteContactUseCase uses withDatabaseTransaction and throws ContactExecutionError (no createAppError import)
  - Updated packages/application/src/index.ts

  Step 4 — Module Restructure
  - Dissolved record/ into components/record/ and controller/
  - Removed application/, domain/, controllers/ (plural), data/server-records.ts, data/server-mutations.ts
  - Rewrote data/queries.ts as bridge to @builders/db

  Step 4.5 — Route Policy Audit
  - All handlers already compliant (applyRoutePolicy, parseMutationEnvelope, receipts, assertExpectedUpdatedAt, routeJson/routeError)

  Step 5 — Import Updates
  - Routes import use cases from @builders/application, types from @builders/domain
  - Components/controllers import types from @builders/domain only
  - Shared engine loader imports listSalesRepContactOptions from @builders/db
  - All 4 test files updated

  Verification
  - 3 packages built clean
  - tsc --noEmit — no new errors (only pre-existing unrelated ones)
  - Grep pass: zero hits for requireRouteAccess, modules/contacts/domain, modules/contacts/application, @/server/http/ in module
  - 20/20 tests passing across 5 files


Checks
- why does the shared/engines/common/transport contain the contact transport data. 
- why does the engine contaom 