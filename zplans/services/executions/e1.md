
  Step 1 - Domain Layer: Created packages/domain/src/flooring/services/types.ts (ServiceRow, ServiceForm, UnitOption, EMPTY_SERVICE_FORM, validateServiceForm, toServiceForm) and barrel index.ts. Updated packages/domain/src/index.ts to export via index barrel instead of direct delete-rules.js.

  Step 2 - Data Layer: Created packages/db/src/flooring/services/ with read-repository.ts (normalizeServiceRow, normalizeServiceOption, listServices, listServiceOptions, getServiceById, getServiceDeleteState) and write-repository.ts (createServiceRecord, updateServiceRecord, deleteServiceRecordById). All functions accept optional client parameter. Write functions normalize through normalizeServiceRow.

  Step 3 - Application Layer: Created packages/application/src/flooring/services/ with errors.ts (ServiceExecutionError), types.ts (ServiceInput, ServiceResult), create-service.ts, update-service.ts, delete-service.ts, and barrel. delete-service.ts uses ServiceExecutionError instead of createAppError (fixed layer violation). Mirrors the contacts/manufacturers pattern.

  Step 4 - Module Restructure: Flattened record/ into components/record/, renamed controllers/ to controller/, rewrote data/queries.ts as bridge to @builders/db (loadUnitOptions now uses listUnitOfMeasures from canonical UoM read repo), deleted all deprecated files (application/, domain/, record/, controllers/, services.ts, data/server-records.ts, components/services-client.tsx re-export shim).

  Step 4.5 - Route Policy Audit: All route handlers already compliant (applyRoutePolicy, parseMutationEnvelope, receipts, assertExpectedUpdatedAt, routeJson/routeError). GET handler uses authorizeServicesRoute — verified it delegates to applyRoutePolicy.

  Step 4.75 - Client-Side Mutation Envelope Fix: Fixed data/mutations.ts with withMutationMeta wrapping on all 3 functions. Wired create client and detail controller to use mutations.ts instead of inline requestJson.

  Step 5 - Import Updates: Rewired all routes to @builders/application, all components/controllers to @builders/domain, dashboard pages to new components/record/ paths, shared engine record-detail-options-loader to @builders/db for listServiceOptions.

  Step 6 - Package Builds: All 3 packages build clean.

  Step 7 - Tests & Verification: Updated 3 test files. Application test (1/1) passes. Client and routes tests have pre-existing vitest module resolution failures (same class of issue as manufacturer tests). tsc --noEmit shows 0 new errors (13 pre-existing unrelated). All grep checks pass zero hits.
