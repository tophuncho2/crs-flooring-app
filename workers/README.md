Background workers will live here once BullMQ is introduced as a separate service.

Planned contents:
- worker bootstrap entrypoint
- queue processors
- domain-specific job handlers
- worker-only instrumentation and concurrency config
