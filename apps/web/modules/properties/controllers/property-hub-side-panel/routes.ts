/**
 * Route opened by the unified "+Template" affordance: a raw new-template form
 * with no property/MC pre-linkage. Owned by the property hub because the hub
 * is the origin of that button, which is shared across every hub-connected
 * panel (the hub side panel itself and the template-sync panel).
 */
export const NEW_TEMPLATE_ROUTE = "/dashboard/templates/new" as const
