// Re-export the canonical 8-column constant so consumers can import from
// `fields/contracts` without reaching into `layout-grid/contracts`.

export { FIELD_SECTION_COLUMNS } from "../../layout-grid/contracts/layout-grid-geometry"
