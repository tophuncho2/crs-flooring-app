// Cross-layer config for the read-only user-activity list view (append-only
// login log). Referenced by both the server-side use case / route validator and
// the client-side data wrapper.

export const LIST_USER_LOGIN_ACTIVITY_PAGE_SIZE = 50
export const LIST_USER_LOGIN_ACTIVITY_MAX_PAGE_SIZE = 200
