// Cross-layer config for the unit-of-measures list view. Declared in domain
// because these values are referenced by both server-side use cases / route
// validators (in @builders/application) and the client-side controller / data
// wrapper (in apps/web/modules/unit-of-measures/).

export const LIST_UNIT_OF_MEASURES_PAGE_SIZE = 50
export const LIST_UNIT_OF_MEASURES_MAX_PAGE_SIZE = 200
