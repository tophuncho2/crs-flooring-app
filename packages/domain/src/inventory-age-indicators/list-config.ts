// Cross-layer config for the inventory-age-indicators list view. Declared in
// domain because these values are referenced by both server-side use cases /
// route validators (in @builders/application) and the client-side controller /
// data wrapper (in apps/web/modules/inventory-age-indicators/).

export const LIST_INVENTORY_AGE_INDICATORS_PAGE_SIZE = 50
export const LIST_INVENTORY_AGE_INDICATORS_MAX_PAGE_SIZE = 200
