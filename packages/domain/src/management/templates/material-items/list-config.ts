// Page-size config for the paginated material-items section inside the
// template preview (rendered in the template-sync side panel). Mirrors the
// list-view list-config.ts pattern: declared in domain so both the use case
// / route validator and the client request helper can read the same constants.

export const TEMPLATE_PREVIEW_ITEMS_PAGE_SIZE = 10
export const TEMPLATE_PREVIEW_ITEMS_MAX_PAGE_SIZE = 50
