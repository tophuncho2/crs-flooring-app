// Cross-layer config for the work-order-document-types list view. Declared in
// domain because these values are referenced by both server-side use cases /
// route validators (@builders/application) and the client-side controller / data
// wrapper (apps/web/modules/work-order-document-types/).

export const LIST_WORK_ORDER_DOCUMENT_TYPES_PAGE_SIZE = 50
export const LIST_WORK_ORDER_DOCUMENT_TYPES_MAX_PAGE_SIZE = 200
