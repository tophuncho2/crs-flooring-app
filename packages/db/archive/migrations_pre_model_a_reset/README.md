This directory is archival only.

It contains the pre-Model-A active migration chain that was retired when `builderswebapp` reset Prisma history into a single fresh baseline migration.

Operational migrations now live exclusively in `packages/db/prisma/migrations`.
