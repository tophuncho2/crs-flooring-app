# Questions
## Answer These Line By Line And I Will Use Them To Operate The Next Pass

1. What is the exact ordered work-order status list from creation to completion?
By default the status will be Open order (change from building order). Then once the worker fufills the order it will be marked to processed. If while being processed there is an inventory shortage it will be marked shortage. user then clicks the button to process again, if good it goes to processed if shortage it stays as shortage. from there it gets marked as invoced and then complete. Users can't go back to open order once marked processed or shortage or complete. 
2. Which of those statuses are manual-only, and which should eventually be worker-owned?
Worker own should definetly be processed, shortage, and complete should be solid. users dont mark an order as open order thats just the default. then there will be a button to process the order which updates the status. if its more efficient and scalable, there can be a seperate status for weather an invoice has been sent. this is not a core requirement to mark a word order complete.
3. When should a shortage be created, updated, cleared, and shown to users?
Shortage status happen when a user processes a work order and one of the material items inventory is inefficient. so only the worker marks this shortage, this will be all worker work mainly i believe. so user sends order, if not enough inventory, no material gets allocated inventory and that material item gets marked shortage and then inherently the work order does as well. then the second time around the user mark that material item as "ignore Invenory" so the processing ignores allocation for that item specifically to process the order or the user needs to add inventory.
4. What does resend or reprocess mean in your preferred workflow: rerun allocation, rerun files, rerun both, or create a new revision? in work orders inventory rows can be de linked from work order panel. inventory rows can be manually linked to orders as well. reprocess means if theres a shortage the workflow didn't do anything yet, then when the user sends again it runs the processing. files are returned from the worker after succussful processing.

5. When a template is synced to a work order, should it overwrite existing rows, merge only missing rows, or support both modes? Template syncs should only be creating new work orders, not editing existing ones. we need to edit this. we need to remove the sync button out from the work orders panel and make it a form button in the header of the work orders table, "Sync" button. this button will be used to now sync templates which creates a new work order, this work order automatically opens up. and i want to add a shared variable per user per table for the row last used by the user is highlighted so that they know where they left off. we will need to add that to each table. and then were also going to need to add a user prefferences page in the user drop down nav for user to turn this on or off. 
6. What fields on a work order should become locked after the order is sent into the future worker flow?
only the date, warehouse and property.
7. What exact event should mark a work order as complete?
by clicking the options drop down in the work order record panel clicking complete then be asked if im sure then clicking yes. 
8. When should analytics update: on send, on allocation success, on job completion, on manual completion, or at more than one point? On job completion
9. Should completed orders live in the main work-order table, a filtered view, or a dedicated completed-orders experience? there will be a dedicated page for completed work orders. but we will also be adding a calender which will show a calender view of all and the tabs will need to be color by sometime of status, maybe an extra field we add to the work orders row just for coloring the calender. but the current status options blend with that slightly. 
10. For inventory allocation, which categories require special location logic and which can follow generic location rules? This varies warehouse to warhouse, carpet and vinyl sheet are the primary candidates however.
11. What files must eventually be generated for a work order, and where should those files attach back into the app? Those files will be added to the work order line, stored in the bucket. those files will be an order slip (handed to the contractor with primary, material and serivice info), there will be a picking ticket to show before and afters for inventory cuts. and there may be an expense sheet.
12. Which failures should block a send completely, and which should allow the order to proceed with shortages or warnings? we need to add a field to work order material items (boolean) for the worker to ignore that row for allocation. unallocated items still show on documents but if no inventory was pulled then it just says the item and shortage. work orders need a property, warehouse, date to be sent. service items dont get marked as shortage. we may have to tighten up this entire worker flow and document everything so its set in stone, especially with the order statu's, but i think one status should be primed for workers another for user display and loggin and calender colors. 
13. Who should be allowed to create `ADMIN` users, `BUILDER` users, and any future narrower roles? builder users have full access, admin users have specific access, and then well need to add another role for warehouse workers with specific acces.
14. What is the intended public registration policy: disabled entirely, invite-only, or builder/admin-created only?disabled to the public this is an internal system.
15. What minimum observability do you want before workers exist in production: job history, retry count, failure reasons, admin job view, alerts, or something else? i need basically full monitoring of the basics before moving to workers.