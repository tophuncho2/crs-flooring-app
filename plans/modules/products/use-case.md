Products Use case

Imports / Inventory
1. Products are added to import inventory staging rows
2. Once import is finalized a worker process adds the staging rows to the inventory. 
3. Inventory rows are linked to to a product

Work Orders
1. Products are added to work orders material items rows. 

Templates
1. Products are added to templates material items row.